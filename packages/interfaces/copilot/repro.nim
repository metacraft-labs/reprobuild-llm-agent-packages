## ``copilot`` — GitHub's Copilot CLI coding agent.
##
## Interface only. **Provenance class: vendor-binary.** GitHub publishes
## release binaries but no corresponding source for the CLI, so only a
## pinned-binary realization is reachable and it must not be described as
## built from source.

import repro_project_dsl

package copilot:
  executable copilot:
    discard
