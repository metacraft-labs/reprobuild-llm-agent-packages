## ``qwen-code`` — Alibaba's Qwen Code CLI coding agent.
##
## Interface only. **Provenance class: source-available** (a fork of the
## Gemini CLI lineage). The member is ``qwen``.

import repro_project_dsl

package `qwen-code`:
  executable qwen:
    discard
