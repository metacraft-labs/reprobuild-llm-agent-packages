## ``gemini-cli`` — Google's Gemini CLI coding agent.
##
## Interface only. **Provenance class: source-available** (Apache-2.0
## TypeScript/Node upstream), so the primary realization is a from-source
## build. The member is ``gemini``.

import repro_project_dsl

package `gemini-cli`:
  executable gemini:
    discard
