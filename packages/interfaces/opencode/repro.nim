## ``opencode`` — the OpenCode terminal coding agent.
##
## Interface only. **Provenance class: source-available**, with the caveat
## the catalog's metadata rules exist for: upstream's licence and its
## published build have diverged before. A realization must record the
## licence it verified at the pinned version rather than inheriting a
## judgement made at some earlier one.

import repro_project_dsl

package opencode:
  executable opencode:
    discard
